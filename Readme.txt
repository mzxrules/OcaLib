My terrible libraries:

source - main OcaLib library
Helper - a collection of helper functions
ZActor - contains hundreds of coded actor definitions. The plan is to create an xml doc instead
         to replace this
XActor - Xml based actor definition stuff. WIP