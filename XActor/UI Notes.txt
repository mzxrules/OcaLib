default = input box, hex
bitflag = sets binary state (no params)
switchflag = sets collectible switch flag (no params)
select = dropdown selector
numupdown = numericUpDown
params:
	unit (display unit)
	increment
	min (integer)		